# Eva

A realistic voice agent that runs entirely on one laptop: open models, no cloud services.

Rebuilt from scratch starting 2026-09-25. The earlier version (cloud voice and brain with a
local fallback) is kept, with its tests, benchmarks and measurements, in `.archive/`; its README
(`.archive/README.md`) says how it ran. `CLAUDE.md` is the project brief.
